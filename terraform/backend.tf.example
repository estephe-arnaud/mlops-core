# Backend Terraform - Configuration GCS
# Instructions: voir README.md section "Déploiement Terraform (GCP)"

terraform {
  backend "gcs" {
    bucket = "YOUR-PROJECT-terraform-state"
    prefix = "mlops-core/terraform.tfstate"
    # encryption_key = "projects/YOUR-PROJECT-ID/locations/global/keyRings/terraform/cryptoKeys/state"  # Optionnel KMS
  }
}
