# ⚠️ IMPORTANT : Ce fichier est un EXEMPLE
# Ne JAMAIS commiter ce fichier avec des valeurs réelles !
# Copiez ce fichier vers secret.yaml et remplissez les valeurs
# Ajoutez secret.yaml à .gitignore
#
# ⚠️ SÉCURITÉ : En production, utilisez plutôt :
# - External Secrets Operator (ESO) avec Secret Manager GCP
# - Sealed Secrets
# - Vault
# - Workload Identity pour accès direct à Secret Manager

apiVersion: v1
kind: Secret
metadata:
  name: iris-api-secrets
  namespace: mlops
  labels:
    app: iris-api
    component: secrets
type: Opaque
# Utiliser stringData pour faciliter l'édition (Kubernetes encode automatiquement en base64)
# Alternative : utiliser 'data' avec valeurs base64 encodées
stringData:
  # API Key pour l'authentification
  # Générer avec: openssl rand -hex 32
  # ⚠️ SÉCURITÉ : Utilisez une clé forte (minimum 32 caractères)
  API_KEY: "your-api-key-here"
  
  # URI MLflow pour charger les modèles depuis GCS
  # Format: gs://bucket-name/mlruns/
  # ⚠️ SÉCURITÉ : Vérifiez que le service account a les permissions nécessaires
  MLFLOW_TRACKING_URI: "gs://your-bucket-name/mlruns/"

