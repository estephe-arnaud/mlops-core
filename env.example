# Configuration de l'environnement pour l'API MLOps
# Copiez ce fichier vers .env et ajustez les valeurs
# ⚠️ Ne commitez JAMAIS le fichier .env (il contient des secrets)

# ============================================================================
# SÉCURITÉ - Authentification API
# ============================================================================
# ⚠️ DÉVELOPPEMENT LOCAL UNIQUEMENT
# Cette variable est utilisée pour le développement local (docker-compose, tests)
# En production sur GCP, l'API_KEY est récupérée automatiquement depuis Secret Manager
# via le startup-script Terraform (pas besoin de .env en production)
#
# Générez une clé sécurisée : openssl rand -hex 32
# Si non configurée, l'authentification est désactivée (mode développement uniquement)
API_KEY=your-secret-api-key-here-change-in-production

# ============================================================================
# CONFIGURATION DU MODÈLE ML
# ============================================================================
# Répertoire contenant le modèle ML (iris_model.pkl), métadonnées (metadata.json) et métriques (metrics.json)
# Par défaut : "models" (relatif au répertoire de travail)
MODEL_DIR=models

# ============================================================================
# CONFIGURATION DE L'ENVIRONNEMENT
# ============================================================================
# Environnement d'exécution : development, staging, production
# Utilisé pour forcer l'authentification en production et configurer CORS
ENVIRONMENT=development

# Origines CORS autorisées (séparées par des virgules)
# En développement : "*" (toutes les origines)
# En production : spécifier les origines exactes (ex: "https://example.com,https://app.example.com")
# ⚠️ SÉCURITÉ : Ne pas utiliser "*" en production
CORS_ORIGINS=*

# Niveau de logging : DEBUG, INFO, WARNING, ERROR, CRITICAL
# Utilisé dans les scripts de déploiement
LOG_LEVEL=INFO
