# ============================================================================
# Configuration de l'environnement pour l'API MLOps Core
# ============================================================================
# 
# 📋 INSTRUCTIONS
# 1. Copiez ce fichier : cp env.example .env
# 2. Ajustez les valeurs selon votre environnement
# 3. ⚠️  NE COMMITEZ JAMAIS le fichier .env (il contient des secrets)
# 4. En production GCP, ces variables sont configurées via Terraform/Secret Manager
#
# 🔗 Documentation : README.md section "Configuration"
# ============================================================================

# ============================================================================
# 🔐 SÉCURITÉ - Authentification API
# ============================================================================
#
# OPTIONNELLE en développement (authentification désactivée si vide)
# OBLIGATOIRE en production (récupérée depuis Secret Manager GCP)
#
# Utile uniquement si vous voulez tester l'authentification en local.
# Pour un développement simple, vous pouvez laisser vide ou commentée.
#
# Générer une clé : openssl rand -hex 32
# Utilisation : curl -H "X-API-Key: your-key" http://localhost:8000/predict
#
API_KEY=

# ============================================================================
# 🌍 CONFIGURATION DE L'ENVIRONNEMENT
# ============================================================================

# Environnement : development, staging, production
# - development : authentification optionnelle, CORS permissif
# - production  : authentification obligatoire, CORS strict
ENVIRONMENT=development

# Origines CORS autorisées (séparées par des virgules, sans espaces)
# ⚠️  C'est le domaine du FRONTEND qui appelle l'API, pas l'API elle-même !
#
# Exemples :
#   CORS_ORIGINS=https://example.com
#   CORS_ORIGINS=https://example.com,https://app.example.com
#   CORS_ORIGINS=http://localhost:3000
#
# ⚠️  En production : NE JAMAIS utiliser "*" (bloqué par sécurité)
# En développement : "*" est autorisé
CORS_ORIGINS=*

# Niveau de logging : DEBUG, INFO, WARNING, ERROR, CRITICAL
# Recommandé : INFO (développement), INFO ou WARNING (production)
LOG_LEVEL=INFO

# ============================================================================
# 🤖 CONFIGURATION DU MODÈLE ML
# ============================================================================

# Répertoire contenant metadata.json et metrics.json
# Le modèle est chargé depuis MLflow via mlflow_run_id dans metadata.json
MODEL_DIR=models

# URI MLflow Tracking Server (optionnel)
# - Développement : laisser vide (utilise mlruns/ local)
# - Production GCP : gs://bucket-name/mlruns/
# - Serveur distant : http://mlflow-server:5000
MLFLOW_TRACKING_URI=

# ============================================================================
# 🐳 DOCKER COMPOSE (Développement Local)
# ============================================================================

# Port d'exposition sur la machine hôte (format: host:port)
# - 127.0.0.1:8000 : localhost uniquement (recommandé en dev)
# - 0.0.0.0:8000   : toutes les interfaces
API_PORT=127.0.0.1:8000
